``` mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: The browser adds the new note to it's in-memory `notes` list and calls <br>`redrawNotes()`which redraws the `notes` list including the new note
    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa: {"message":"note created"}
    activate server
    server-->>browser: 201 Created
    deactivate server
```